__pycache__/
*.pyc
.DS_Store
.venv/
.env
*.log

# Credenciais: `.env` sozinho casa só o basename exato. Qualquer arquivo
# terminado em .env (instagram.env, gemini.env...) também fica de fora.
*.env
# Backups do env gerados pela renovação do token do Instagram.
*.env.bak.*
# LaunchAgents renderizados com caminhos absolutos da máquina do aluno.
*.plist

# Os moldes públicos continuam versionados (sufixo diferente):
!*.env.example
!*.plist.template

# Saída do /graphify — artefato de análise local, nunca versionar (contém o
# grafo inteiro do repo, incluindo nomes de skills e infra internos).
graphify-out/

# Histórico de sessão do Claude: é nota INTERNA de trabalho do Rafael,
# não material do aluno. Repo é PÚBLICO.
HISTORICO-SESSOES.md
